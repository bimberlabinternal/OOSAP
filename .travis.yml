# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: required
cache: packages
use_bioc: true

os:
  - linux
  - osx

r: release

bioc_packages:
  DropletUtils