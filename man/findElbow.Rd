% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Seurat_III.R, R/mcl.R
\name{FindElbow}
\alias{FindElbow}
\title{A Title}
\usage{
FindElbow(y, plot = FALSE, returnIndex = TRUE, ignore.concavity = F,
  min.y = NA, min.x = NA)

FindElbow(y, plot = FALSE, returnIndex = TRUE, ignore.concavity = F,
  min.y = NA, min.x = NA)
}
\arguments{
\item{SeurObj, }{A Seurat object.}
}
\value{
A modified Seurat object.
}
\description{
A description
}
\examples{
tmp <- FindElbow(c(0.9, 1.1, 1.1, 1.9, 2.5, 2.8, 4.9, 8.5),
	plot = TRUE) # wandering
tmp <- FindElbow(c(0.9, 1.0, 1.2, 1.3, 1.5, 1.5, 10.0, 22.0),
	plot = TRUE) # late rise
tmp <- FindElbow(c(2, 4, 6, 8, 10, 12, 14, 16)^2,
	plot = TRUE) # gradual, no obvious break

# Not the usual way to choose the number of PCs:
library("chemometrics")
data(glass)
pca <- prcomp(glass)
eigensum <- sum(pca$sdev * pca$sdev)
vv <- 100 * (pca$sdev * pca$sdev/eigensum)
cs <- cumsum(vv)
tmp <- FindElbow(vv, plot = TRUE)
tmp <- FindElbow(cs, plot = TRUE)

tmp <- FindElbow(c(0.9, 1.1, 1.1, 1.9, 2.5, 2.8, 4.9, 8.5),
	plot = TRUE) # wandering
tmp <- FindElbow(c(0.9, 1.0, 1.2, 1.3, 1.5, 1.5, 10.0, 22.0),
	plot = TRUE) # late rise
tmp <- FindElbow(c(2, 4, 6, 8, 10, 12, 14, 16)^2,
	plot = TRUE) # gradual, no obvious break

# Not the usual way to choose the number of PCs:
library("chemometrics")
data(glass)
pca <- prcomp(glass)
eigensum <- sum(pca$sdev * pca$sdev)
vv <- 100 * (pca$sdev * pca$sdev/eigensum)
cs <- cumsum(vv)
tmp <- FindElbow(vv, plot = TRUE)
tmp <- FindElbow(cs, plot = TRUE)

}
\keyword{SerIII_template}
